apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: antattack1
  name: antattack1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: antattack1
  template:
    metadata:
      labels:
        app: antattack1
    spec:
      containers:
      - image: nginx:latest
        name: nginx
        ports:
        - containerPort: 80
          name: nginx